# Install Backstage with the Scorecards plugin enabled

Create kind cluster (ensure project contour is enabled to use the internal Ingress)

Install using helm with this command

On x86:

```shell
helm install . scorecards
```



